ProlifeLanding

Mission

Это коммерческий landing page для доставки грузов из Китая в Бишкек.
Главная цель — рост конверсии в лиды и заявки.

⸻

Language
	•	Всегда отвечай пользователю на русском языке
	•	Все объяснения, планы, summary и комментарии пиши на русском
	•	Код и технические идентификаторы оставляй на языке проекта

⸻

Product Priorities
	1.	Конверсия в лид важнее визуальной избыточности
	2.	Простота и ясность важнее сложных эффектов
	3.	Hero, calculator, CTA, trust blocks — зоны наивысшего приоритета
	4.	Любые изменения должны сохранять быстрый рендер и адаптивность

⸻

🚨 CRITICAL AGENT BEHAVIOR RULES (MANDATORY)

Ты работаешь как production-инженер, а не как экспериментальный ассистент.

НИКОГДА НЕ ДЕЛАЙ:
	•	НЕ используй git push --force
	•	НЕ используй git reset --hard без явного запроса
	•	НЕ переписывай историю git
	•	НЕ выполняй деструктивные команды без подтверждения
	•	НЕ пробуй “наугад” решения при ошибках

ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ:

Ты обязан:
	1.	Остановиться
	2.	Объяснить причину
	3.	Предложить план исправления
	4.	Дождаться подтверждения

⸻

Engineering Rules
	•	Никогда не пушь напрямую в main
	•	Всегда работай через новую feature-ветку
	•	Перед коммитом обязательно запускай build
	•	Перед push показывай список измененных файлов
	•	Перед PR коротко объясняй что изменилось и зачем
	•	Не меняй архитектуру без явной необходимости
	•	Избегай лишних зависимостей
	•	Не редактируй secrets, env-файлы и конфигурации деплоя без явного запроса

⸻

Git Workflow (STRICT)

Для любой кодовой задачи:
	1.	Анализ задачи
	2.	План изменений
	3.	Подтверждение плана
	4.	Создание ветки:

git checkout main
git pull
git checkout -b feature/<short-name>

	5.	Внесение изменений
	6.	Локальная проверка
	7.	Commit
	8.	Push
	9.	PR

⸻

🚨 Repository Hygiene (CRITICAL)

НИКОГДА НЕ ДОБАВЛЯЙ В GIT:
	•	node_modules/
	•	dist/
	•	.vite/
	•	build/
	•	.env*
	•	любые сгенерированные файлы

Если такие файлы появились:
→ ОСТАНОВИСЬ и сообщи пользователю

⸻

Required Checks Before Commit

Обязательно перед коммитом:

npm run build

И проверить:
	•	нет случайных изменений
	•	не изменились node_modules
	•	не изменился dist
	•	изменения относятся только к задаче

⸻

Package Management Rules

Если изменился package-lock.json:

Ты обязан:
	•	объяснить причину
	•	подтвердить, что зависимости менялись осознанно

НЕЛЬЗЯ коммитить lock-файл без объяснения

⸻

File Change Protocol

Перед изменением любого файла:

Ты обязан:
	•	объяснить что будешь менять
	•	объяснить зачем
	•	описать ожидаемый результат

После изменений:
	•	кратко описать что сделано

⸻

UX/CRO Rules
	•	Каждый важный CTA должен быть понятен с первого взгляда
	•	Калькулятор должен не только считать, но и помогать забирать лид
	•	Любой новый текст должен быть простым, конкретным и коммерчески понятным
	•	Избегать абстрактных заголовков без выгоды
	•	Сначала предлагать минимальные изменения с максимальным эффектом

⸻

Acceptance Criteria

Любая задача считается выполненной только если:
	•	проект собирается без ошибок
	•	UI не ломается на desktop и mobile
	•	не сломан текущий путь до лида
	•	изменения локализованы и понятны
	•	подготовлен понятный commit message
	•	подготовлен PR summary

⸻

🚨 Failure Handling

Если ты не уверен:
	•	НЕ угадывай
	•	НЕ импровизируй
	•	НЕ делай рискованные изменения
	•	Спроси пользователя

⸻

PR Cleanup Protocol

После создания PR и после merge Claude должен проверять состояние веток и PR.

Rules
	•	Если PR показывает 0 files changed, Claude должен:
	1.	сообщить, что PR пустой
	2.	НЕ пытаться его merge
	3.	предложить закрыть его
	4.	после подтверждения закрыть PR
	5.	удалить связанную feature-ветку локально и remote, если она больше не нужна
	•	После успешного merge в main, Claude должен:
	1.	проверить, что PR merged
	2.	предложить cleanup
	3.	удалить feature-ветку локально
	4.	удалить feature-ветку на origin
	5.	выполнить git fetch --prune
	6.	показать итоговый список оставшихся веток
	•	Claude всегда должен считать main единственной долгоживущей веткой
	•	Все feature-ветки — временные и должны удаляться после merge или если PR стал пустым

⸻

Cleanup Acceptance Criteria
	•	не осталось пустых PR
	•	не осталось ненужных feature-веток
	•	локальные ветки синхронизированы с origin
	•	git branch и remote branches чистые